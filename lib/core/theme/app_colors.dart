import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFE50914); // Netflix-like red
  static const Color background = Color(0xFF0F172A); // Dark slate
  static const Color surface = Color(0xFF1E293B);
  
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF94A3B8);
  
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  
  // Seats
  static const Color seatAvailable = Color(0xFF334155);
  static const Color seatSelected = primary;
  static const Color seatBooked = Color(0xFF64748B);
}
